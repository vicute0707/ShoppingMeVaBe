spring.application.name=ShoppingMomadnBaby

# ==========================================
# SERVER CONFIGURATION
# ==========================================
server.port=8081
server.servlet.session.timeout=30m
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json

app.base-url=https://presophomore-adjunctly-margery.ngrok-free.dev

# ==========================================
# DATABASE CONFIGURATION - MariaDB
# ==========================================
spring.datasource.url=jdbc:mariadb://localhost:3306/shop_me_va_be?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=sapassword

# HikariCP Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# ==========================================
# JPA/HIBERNATE CONFIGURATION
# ==========================================
# Use 'update' for development, 'validate' for production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# SQL Initialization - Load data from data.sql
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never
# NOTE: Use data.sql manually via MySQL/MariaDB client to avoid duplicate data
# Command: mysql -u root -p shop_me_va_be < src/main/resources/db/data.sql

# ==========================================
# JSP CONFIGURATION
# ==========================================
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# ==========================================
# EMAIL CONFIGURATION (Gmail)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenthituongvi2023@gmail.com
spring.mail.password=hkpw hzun lrnh xuxv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================================
# MOMO PAYMENT GATEWAY CONFIGURATION
# ==========================================
# Test Environment
momo.endpoint=https://test-payment.momo.vn/v2/gateway/api/create
momo.partner-code=MOMO
momo.access-key=F8BBA842ECF85
momo.secret-key=K951B6PE1waDMi640xX08PD3vg6EkVlz
momo.redirect-url=${app.base-url}/payment/momo/callback
momo.ipn-url=${app.base-url}/payment/momo/ipn
momo.request-type=captureWallet

# ==========================================
# JWT CONFIGURATION
# ==========================================
# JWT Secret Key (min 256 bits / 32 characters)
jwt.secret=ShopMeVaBe2025SecretKeyForJWTAuthenticationVeryLongAndSecure123456789
# JWT Expiration: 24 hours (in milliseconds)
jwt.expiration=86400000

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
# Development mode - detailed logging
logging.level.root=INFO
logging.level.iuh.student.www=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ==========================================
# SECURITY CONFIGURATION
# ==========================================
# CSRF protection for forms
spring.security.csrf.cookie.http-only=true
spring.security.csrf.cookie.secure=false

# ==========================================
# ACTUATOR (Health Monitoring)
# ==========================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ==========================================
# PERFORMANCE OPTIMIZATION
# ==========================================
# Jackson JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Ho_Chi_Minh

# Static resources caching
spring.web.resources.cache.cachecontrol.max-age=3600

# ==========================================
# CLOUDINARY IMAGE UPLOAD
# ==========================================
# Enable/disable Cloudinary (set to true to use Cloudinary, false for local storage)
cloudinary.enabled=true

# Cloudinary credentials - Get from https://cloudinary.com/console
# Sign up free at: https://cloudinary.com/users/register/free
# IMPORTANT: Cloud name KHÔNG có ký tự @ ở đầu!
cloudinary.cloud-name=dubthm5m6
cloudinary.api-key=214531891487818
cloudinary.api-secret=NKYTxvXdkPNSgNetWJduDEB0N84

# File upload settings
file.upload-dir=src/main/resources/static/uploads/products
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ==========================================
# ERROR HANDLING
# ==========================================
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
